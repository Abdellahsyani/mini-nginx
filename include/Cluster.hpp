#ifndef CLUSTER_HPP
#define CLUSTER_HPP

class Cluster {
public:
  // Constructors
  // ==============
  Cluster();
  Cluster(Cluster& other);
  Cluster& operator=(Cluster& other);
  ~Cluster();

  // Methods
  // =========

};

#endif // CLUSTER_HPP!

