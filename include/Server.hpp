#ifndef SERVER_HPP
#define SERVER_HPP

class Server {
public:
  // Constructors
  // ==============
  Server();
  Server(Server& other);
  Server& operator=(Server& other);
  ~Server();

  // Methods
  // =========

};

#endif // SERVER_HPP!
