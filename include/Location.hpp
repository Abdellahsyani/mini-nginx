#ifndef LOCATION_HPP
#define LOCATION_HPP

class Location : public ServerConfig {
public:
  // Constructors
  // ==============
  Location();
  Location(Location& other);
  Location& operator=(Location& other);
  ~Location();

  // Methods
  // =========

};

#endif // LOCATION_HPP!
